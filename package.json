{
  "name": "salesops-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "prisma": {
    "schema": "server/prisma/schema.prisma"
  },
  "scripts": {
    "setup": "npm install && npm run prisma:generate -w server && npm run prisma:migrate -w server",
    "client:dev": "npm run dev -w client",
    "server:dev": "npm run dev -w server",
    "dev": "concurrently \"npm run client:dev\" \"npm run server:dev\"",
    "start": "npm run prisma:generate -w server && npm run prisma:migrate -w server && npm run dev",
    "build": "npm run build -w client && npm run build -w server",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
